# Section VII D-F: Global numerical geometry

Fit pairwise maps in the exact `L` spaces, synchronize one orientation and scale
per condition, and test held-out relations and held-out conditions.

```bash
python experiments/04_global_geometry/pairwise_geometry.py --help
python experiments/04_global_geometry/synchronize_frames.py --help
python experiments/04_global_geometry/factorized_paths.py --help
```

The scripts read the trained DAS checkpoints and write summaries below
`results/final_exps/` and `results/experiments/closing/`.
