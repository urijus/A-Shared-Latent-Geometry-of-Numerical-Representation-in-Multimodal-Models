# Section VII A-C: Readout and latent geometry

These analyses measure digit-readout overlap, separate the exact readout-coupled
`C` and readout-orthogonal `L` components, and evaluate the geometry that remains.

```bash
python experiments/03_readout_latent_geometry/readout_audit.py --help
python experiments/03_readout_latent_geometry/readout_free_geometry.py --help
```

Outputs retain the original paths below `results/experiments/` and
`results/closing/`.
